stream_chat()

      │
      ▼
IntentRouter

      │
      ├── chat
      │      │
      │      ▼
      │   Normal LLM
      │
      └── execute
             │
             ▼
      ExecutionService

will put a intent router middle layer that will determine whether the user response need a simple llm response or planned execution