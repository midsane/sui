Execution Session

WAITING_FOR_REQUIREMENTS
            │
            ▼
PLANNING
>(requirement gathering)
RequirementResult(
    complete=True,
    task="Build a restaurant website...",
    project_dir="./food",
)

>(execution planning step wise)
ExecutionPlan(
    goal=...
    steps=[
        ...
    ]
)


            │
            ▼
WAITING_FOR_APPROVAL
            │
            ▼
EXECUTING
            │
            ▼
> execution happens in steps like this 

        ↓

    mkdir tool

        ↓

    Create file

        ↓

    file_write tool

        ↓

    Run npm install

        ↓

    terminal tool -->


            │
            ▼

4. Evaluation

After each step (or every few steps), ask:

Did the step succeed?

If not,

return failure


5. REFLECTION
5. Reflection

If evaluation fails:

Why?
↓

Generate a fix.
↓
Return corrected plan.
This is where autonomous retries live. ( i might implement it later)